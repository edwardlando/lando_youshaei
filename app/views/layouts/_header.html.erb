


<div class="top" style="padding-top:0px; margin-top:0px;">
	<div class="container" style="padding-top:30px; margin-top: 0px;">
		<div id="user nav">
			<p class="title"><%= link_to "AVEECE", {:controller => "store", :action => "index", :index => current_user.current_channel.item_index } %></p>
			<strong><%- current_user.email %></strong>
			<% unless current_user.nil? %>
				<% if current_user.role == "tastemaker" || current_user.role == "admin" %>
					<div class="add" id="new_item_button">+ Item</div>
				<% end %>
					<div class="add" id="new_channel_button">+ Channel</div>
					<% unless current_user.current_channel.channel_items.empty? %>
						<div class="add" id="add_to_cart_link">+ Cart</div>  
						<div class="add"><%= link_to "+ Wishlist", {:controller => "users", :action => "add_to_wishlist" } %></div> 
					<% end %>
					<div class="add" id="my_profile">Profile</div>
					<div class="add" id="my_cart">Cart</div>
			<% end %>
			<p class="next"> <%= link_to "Next", {:controller => "store", :action => "index", :index => current_user.current_channel.item_index+1 } %></p>
		</div>
	</div>
</div>